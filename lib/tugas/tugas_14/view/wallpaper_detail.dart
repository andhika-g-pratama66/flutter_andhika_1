import 'package:flutter/material.dart';

class WallpaperDescription extends StatelessWidget {
  const WallpaperDescription({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar());
  }
}
